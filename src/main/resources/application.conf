app {
  port = 8080
  port = ${?PORT}
}

db {
  host     = "localhost"
  host     = ${?DB_HOST}
  port     = 5432
  port     = ${?DB_PORT}
  name     = "flashcards"
  name     = ${?DB_NAME}
  user     = "postgres"
  user     = ${?DB_USER}
  password = "postgres"
  password = ${?DB_PASSWORD}
}
